<div class="row mt-15">
  <div class="col-md-12 form-inline">
    <%= image_tag comment.user.avatar, width: 30, height: 30 %>
    <strong><%= link_to comment.user.full_name, user_path(comment.user) %></strong>
      <small> <%= comment.content %></small>
  </div>
</div>
